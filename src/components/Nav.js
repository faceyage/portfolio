function Nav({ name }) {
  return (
    <header>
      <div className="logo">{name}</div>
      <nav>
        <a href="">Portfolio</a>
        <a href="">About</a>
        <a href="">Contact</a>
      </nav>
    </header>
  );
}

export default Nav;
//!work in progress
